

export const signup = async ({ firstName, lastName, email, password }) => {

  const user = {}
  return user;
};

export const logout = () => {
  
};

export const login = async ({ email, password }) => {
  const resp = {};

  return resp.user;
};
